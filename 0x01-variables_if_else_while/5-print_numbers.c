#include <stdio.h>

/**
 * main - program that prints all singlr digit numbers of base 10
 * Return: always 0
 */

int main(void)
{
	char i;

	for (i = 0; i < 10; i++)
	putchar(i);
	putchar('\n');

	return (0);
}
