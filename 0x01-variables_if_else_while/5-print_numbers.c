#include <stdio.h>

/**
 * main - program that prints all singlr digit numbers of base 10
 * Return: always 0
 */

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
