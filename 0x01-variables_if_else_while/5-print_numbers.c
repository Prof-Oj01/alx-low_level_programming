#include <stdio.h>

/**
 * main - program that prints all singlr digit numbers of base 10
 * Return: always 0
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
