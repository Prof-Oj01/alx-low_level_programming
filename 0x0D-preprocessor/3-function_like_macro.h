#ifndef LIKE_MACRO_H
#define LIKE_MACRO_H

/**
 * File: 3-function_like_macro.h
 * @x: input value, int
 * Desc: header file that computes thr absolute value of a num x
 */

#define ABS(x) ((x) < (0) ? -(1) * (x) : (x))

#endif
